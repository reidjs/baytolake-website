# Bay To Lake
To run local:
1. cd into the `website` directory
2. ./serve.sh`
3. open browser to localhost:8000

To deploy to baytolake.org (Reid only for now)
1. run ./deploy.sh
2. open https://baytolake.org/
